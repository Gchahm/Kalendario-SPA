import {RequestCardComponent} from '@customers/components/request-card/request-card.component';

describe('AppointmentCardComponent', () => {
  const component = new RequestCardComponent(null);

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
