import { ToMomentDateLongPipe } from './to-moment-date-long.pipe';

describe('ToMomentDateLongPipe', () => {
  it('create an instance', () => {
    const pipe = new ToMomentDateLongPipe();
    expect(pipe).toBeTruthy();
  });
});
